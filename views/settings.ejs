<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>設定ページ</title>
  <!-- BootstrapのCSS読み込み -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- ナビゲーションバー -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">XML Generator</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <!-- メインページへのリンク -->
          <li class="nav-item">
            <a class="nav-link" href="/">メインページ</a>
          </li>
          <!-- 設定ページへのリンク（現在のページ） -->
          <li class="nav-item">
            <a class="nav-link active" href="/settings">設定</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container mt-5">
    <h1 class="text-center mb-4">設定ページ</h1>
    <form id="settingsForm" method="POST" action="/settings" class="p-4 border rounded">
      <div class="mb-3">
        <label for="filePath" class="form-label">ファイルパス:</label>
        <input 
          type="text" 
          class="form-control" 
          id="filePath" 
          name="filePath" 
          value="<%= config.filePath %>" 
          required>
      </div>
      <div class="mb-3">
        <label for="MedicalInstitutionCode" class="form-label">医療機関コード:</label>
        <input 
          type="text" 
          class="form-control" 
          id="MedicalInstitutionCode" 
          name="MedicalInstitutionCode" 
          value="<%= config.MedicalInstitutionCode %>" 
          required>
      </div>
      <div class="d-flex justify-content-center">
        <button type="submit" class="btn btn-primary me-2">保存</button>
        <a href="/" class="btn btn-secondary">キャンセル</a>
      </div>
    </form>
  </div>
  <!-- BootstrapのJS読み込み -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
